# .semgrepignore
# Exclude internal pipeline tooling scripts from Semgrep SAST scan.
# These scripts make HTTPS-only API calls (NVIDIA, Groq, Gemini, GitHub)
# with explicit URL validation guards — false positives for dynamic-urllib rule.
# Exclude vulnerability test file — intentionally contains insecure patterns
# for pipeline validation purposes.

scripts/ai-code-review.py
scripts/ai-auto-fix.py
